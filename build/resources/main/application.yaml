server:
  port: 8080
spring:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  application:
    name: ProductRez
  datasource:
    url: jdbc:postgresql://localhost:5432/epm
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 12345
